import Dropdown from "./Dropdown";

const meta = {
  component: Dropdown,
};

export default meta;

export const Default = {
  args: {
    label: "Label",
    labelVisibility: "Visible",
    labelIconVisibility: "Hidden",
  },
};
